# apps/mobile — Expo (iOS / Android / Tablet)

**Status: stub.** Activated in Phase 1+ once COURT and COURIER land.

This will be the Expo / React Native app sharing `packages/core`, `packages/auth`,
and the design tokens from `packages/ui`. It adds push, voice commands, offline
sync, quick approvals, biometrics, widgets, and voice notes on top of feature
parity with the web HQ.

Do not scaffold the Expo project until Phase 1 — see the root `README.md` build table.
